source 'http://rubygems.org'

gem 'rails', '3.0.19'
gem 'mysql2', '0.2.7'
#gem 'mysql'
# set the authorization gem
gem 'devise', '1.5.2'

#jquery
gem 'jquery-rails', '1.0.19'

#authorize .net
gem 'nokogiri', '1.5.0'
gem 'authorize-net', '1.5.2' #please download the file from authorize .net site

#paperclip
gem 'paperclip', '2.4.5'

# geocoder: handle all the information related with the geolocalization
gem "geocoder"

# adding the gem that paginates the requests
gem 'will_paginate'

# allow the forms to use AJAX to upload images
gem "remotipart", "~> 1.0"

# Deploy with Capistrano
gem 'capistrano'
#gem "rmagick", "1.15.17"

#add Resque-based background worker to send Apple Push Notifications
gem "apn_sender", "1.0.5"
gem "redis", "2.2.2"
#gem 'google_places'

gem 'httparty'

# To use debugger (ruby-debug for Ruby 1.8.7+, ruby-debug19 for Ruby 1.9.2+)
# gem 'ruby-debug'
gem 'debugger', :require => 'debugger'

# Bundle the extra gems:
# gem 'bj'
# gem 'nokogiri'
# gem 'sqlite3-ruby', :require => 'sqlite3'
# gem 'aws-s3', :require => 'aws/s3'

# Bundle gems for the local environment. Make sure to
# put test-only gems in this group so their generators
# and rake tasks are available in development mode:
# group :development, :test do
#   gem 'webrat'
# end
